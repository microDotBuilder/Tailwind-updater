import { describe, it, expect } from "vitest";
// import { interactiveMode } from "../../helpers/intractive-mode";
describe("Interactive Mode", () => {
  it("should return the current directory if the user is in the right directory", async () => {
    expect(1).toBe(1);
  });
});
